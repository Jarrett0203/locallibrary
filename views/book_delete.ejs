<!DOCTYPE html>
<html lang="en">

<head>
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css" />
</head>

<body>
  <h1><%= title %>: <%= book.title %> </h1>
  <% if (bookInstances.length == 0) { %>
  <p>Do you really want to delete this book?</p>
  <form action="" method="post">
    <div class="form-group">
      <input type="hidden" id="bookId" class="form-control" name="bookId" required="true" value=<%= book._id %>>
    </div>
    <button class="btn btn-primary" type="submit">Delete</button>
  </form>
  <% } else { %>
  <p><strong>Delete the following book instances before deleting this book.</strong></p>
  <h4>Book Instances:</h4>
  <dl>
    <% bookInstances.forEach(bookInstance => { %>
    <dt>
      <a href="<%= bookInstance.url %>"> <%= bookInstance.imprint %> : <%= bookInstance.status %> </a>
    </dt>
    <% }) %>
  </dl>
  <% } %>
</body>